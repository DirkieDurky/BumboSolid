@model BumboSolid.Data.Models.Shift

<header class="d-flex align-items-center justify-content-center">
    <div class="text-center text-nowrap">
        <h1>Verwijderen</h1>
        <br />
        <h4 class="text-center">Weet je zeker dat je deze shift wil verwijderen?</h4>
    </div>
</header>

<!-- TODO show data-->
<br />

<div class="d-flex row justify-content-center">
    <div class="d-flex flex-row justify-content-around w-50">
        <a class="btn bumbo-btn" asp-action="Edit" asp-route-id="@Model.Id">Terug</a>
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Verwijderen" class="btn bumbo-btn bumbo-btn-danger" />
        </form>
    </div>
</div>
